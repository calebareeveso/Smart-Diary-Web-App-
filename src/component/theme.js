import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#32AEE3",
    },
    secondary: {
      main: "#f1f1f1",
    },
    neutral: {
        main: '#32AEE3',
      },
    white: {
        main: '#f1f1f1',
      },
  },
});

export default theme;